name: flutter_node_crud_frontend
description: A minimal Flutter app connected to Node.js backend.
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  flutter_lints: ^3.0.1
  provider: ^6.1.4
  google_fonts: ^6.1.0
  glassmorphism: ^2.0.0

flutter:
  uses-material-design: true
